import { Title, Text, Anchor, Button, Navbar, Header, Group, MantineGradient } from '@mantine/core';

export default () => (
  <Group style={{ marginLeft: "30vw", marginRight: "30vw" }} direction={'column'}>
  </Group>
);
